= simple_form_for [handout.parent.becomes_content, handout] do |f|
  = f.input :name
  = f.input :data
  .actions
    = f.button :submit, "保存"
    = link_to 'キャンセル', {:action => :show}, :class => 'btn'
