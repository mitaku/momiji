- objects.each do |obj|
  .object_area.line-bottom
    .image_area= image_tag "resource-48x48.png"
    .info_area
      .info.first
        %i.icon-user
        %span= obj.name
      .info.second
    .action_area
      = link_to [target_company.to_company, obj] do
        %i.icon-book
